<nav class="navbar fixed-top navbar-light bg-light" style="top:54px; z-index: 500;">
    <span class="navbar-text">
  
    <h5 class="mb-0">
      <% if @parent_message %>
        <a href="<%= @origin != nil ? @origin : conversation_path(@parent_message.conversation_id) %>#bottom" class="btn btn-primary btn-sm mr-1">Zurück in den Hauptchat</a>
      <% end -%>
      <%= title if defined? title %>
    </h5>
  </span>
</nav>


<div class="row" id="cable_conversation" data-behavior="cable_conversation" data-conversation-id="<%= conversation.id %>" style="padding-top: 40px; padding-bottom: 80px;">
  <div class="col-12" id="messages">     
    <% if @parent_message %>
      <div class="px-3 pt-3 rounded bg-light mb-2">
        <h6>Ausgangsnachricht</h6>
        <%= render @parent_message, hide_thread_link: true %></div>
    <% end -%>
    
    <%= render messages %>
    <div  id="bottom"></div>
  </div>
</div>

<div class="fixed-bottom">
  <div class="container">
    <%= render "messages/form", conversation: conversation, parent_message: @parent_message %>
  </div>
</div>
<script type="text/javascript">
  // 
  Turbolinks.clearCache();
</script>