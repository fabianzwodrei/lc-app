 <h3>
  Qualifikationen
 </h3>
    Aktueller Status:&nbsp;
    <%= render partial: 'qualification_state', :formats => [:html], locals: {member: @member} %>

    
    <% courses = @member.courses.passed %>
      <% if courses.any? %>
      <br>
      <br>
      <h6 style="color:darkslategray">
        <span data-toggle="tooltip"
              title="Hier werden alle deine erfolgreich absolvierten Schulungen aufgelistet. Bei Modul 2 Schulungen, die nicht älter als ein Jahr sind, wird zusätzlich noch das Ablaufdatum aufgeführt."
              data-placement="top"> 
          Erfolgreich absolvierte Schulungen
        </span>
      </h6>
      <table class="table table-sm" id="qualifications" width="100%">
        <% for course in courses %>
          <tr>
            <td class="text-muted"><%= print_course_type course %></td>
            <td class="title"><%= course.title %></td>
            <td class="small qualification_validity text-muted">
              <%= print_qualification_validity course %>
            </td>
          </tr>
        <% end %>
      </table>
    <% end -%>